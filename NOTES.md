# 작업 노트

## 최근 변경

- 2026-04-26: **post-v3: 오픈 이슈 라벨 한글화** — `i18n/ko.json` `open_issues_label`: "OPEN ISSUES" → "열린 이슈" (`insight_open_issues` 와 일관). `ProjectCard.astro` 의 `.card-open-issues` 마크업에서 `[숫자, 라벨]` 순서를 DetailHeader 와 동일하게 `[라벨, 숫자]` 로 변경 — ko 어순 "열린 이슈 1" 자연스럽게 + en 도 카드/상세 일관 ("OPEN ISSUES 1"). en 라벨은 SCREAMING_CASE 다른 라벨과 일관성 유지를 위해 변경 X. 빌드 10.12s 통과.

- 2026-04-26: **post-v3: 카드/상세 오픈 이슈 표시** (사용자 "권장안으로 진행" 묶음 승인 — 공개 타입 2건 확장 + 마크업 변경 + DetailHeader props 시그니처 변경). `types.ts` `RepoCardData.openIssues: number` + `RepoDetailData.openIssues: number` 추가. `data.ts` `buildCardData` + `getRepoDetail` 가 `repo.open_issues_count ?? 0` 채움. `i18n/ko.json`+`en.json` `open_issues_label`: 둘 다 "OPEN ISSUES"(SCREAMING_CASE, 다른 라벨과 일관). `ProjectCard.astro` 카드 본문 마지막에 `.card-footer` flex space-between wrapper 신설(좌 `.card-updated` + 우 `.card-open-issues`), **`card.openIssues > 0` 가드로 0 카드는 미렌더**(시각 노이즈 회피, plan §9 권장안). `DetailHeader.astro` props `openIssues: number` + `repoUrl: string` 추가, 상태 pill / 버전 줄에 `<a class="detail-open-issues" href={repoUrl + '/issues'} target="_blank" rel="noopener noreferrer" aria-label="N open issues on GitHub">` 추가, **0도 표시**(단일 저장소 dashboard 일관성). `[repo].astro` DetailHeader 호출에 `openIssues={detail.openIssues}` + `repoUrl={detail.url}` 추가. `global.css` `.card-footer`(flex space-between gap 12), `.card-open-issues`(text-muted 본문 + emerald 숫자), `.detail-open-issues`(border + hover emerald 0.45, padding 2x8, 0.25rem radius — version-label 톤과 일관). 검증: 실데이터 빌드 10.07s 통과, dist/index.html 에 `.card-footer`+`.card-open-issues` 마크업 3건(AppGroup open=1 정상 표시 + MailTrayNotifier open=0 미렌더 폴백 정상), dist/{MailTrayNotifier,AppGroup}/index.html 에 `<a class="detail-open-issues" href="https://github.com/jongcheol-pak/{repo}/issues">` 정상 emit (각각 0/1 모두 표시). MOCK 빌드 통과(카드/상세 0건). 회귀 토큰 모두 유지. 색상은 무채색 텍스트 + 숫자만 emerald 강조(`--danger` 강조는 InsightsPanel 합계에서만 유지 — 권장안 결정).

- 2026-04-26: **plan v3 Phase 10 (C-3) — Lighthouse 점수 README 뱃지 자동 갱신** (승인 9, plan v3 단일화: 자동 PR 생성 비채택 + commit message prefix `[lighthouse]` 가드 1방식 + `paths-ignore` 비채택). README.md 상단에 `<!-- LIGHTHOUSE:START --> | Performance | Accessibility | Best Practices | SEO | ... <!-- LIGHTHOUSE:END -->` 마커 블록 추가(초기값 `--`, 첫 lighthouse run 후 자동 갱신). `.github/workflows/lighthouse.yml` 갱신 — `permissions: contents: write` + checkout `ref: main` `fetch-depth: 0` + treosh/lighthouse-ci-action@v12 후 `Update README badge` step 에서 `.lighthouseci/lhr-*.json` 을 jq 로 파싱(`(.categories.{performance|accessibility|best-practices|seo}.score * 100) | round`) 후 python re.sub 로 마커 사이 markdown table 치환(+ `_Lighthouse 점수 자동 갱신: YYYY-MM-DD (UTC)_` 캡션) + `Commit README badge` step 에서 `git commit -m "[lighthouse] update README badge $(date -u +'%Y-%m-%d')" && git push origin HEAD:main`. `.github/workflows/deploy.yml` build job 에 `if: ${{ !startsWith(github.event.head_commit.message, '[lighthouse]') && !startsWith(github.event.head_commit.message, '[skip ci]') }}` 가드 추가(head_commit 은 push 이벤트에만 존재, schedule/workflow_dispatch 는 null → if 통과 = 정상 실행). deploy job 은 needs:build 라 build skip 시 자동 skip(별도 if 불필요). 검증: 두 워크플로우 모두 js-yaml 파싱 통과(deploy: jobs build/deploy, lighthouse: jobs lighthouse), README 마커 블록 정상, deploy.yml 가드 startsWith 1건. **CI 검증 한계**: 워크플로우 1회 trigger 후 README 갱신 + deploy.yml 추가 trigger 차단 검증은 다음 배포 후 사용자 확인. 회귀: 빌드 9.44s 통과.

- 2026-04-26: **plan v3 Phase 9 (C-2) — Playwright E2E 스모크 테스트** (승인 8). `package.json` `devDependencies` `@playwright/test ^1.59.1` 추가 + scripts `"test:e2e": "playwright test"`. `npx playwright install chromium` 으로 Chromium + Winldd 바이너리 설치(로컬 `~/AppData/Local/ms-playwright/`). 신규 `playwright.config.ts` — `testDir: ./tests/e2e`, `baseURL: http://localhost:4321`, chromium project 1개만, `webServer.command: 'npm run preview'` (사전 build 필요), `reuseExistingServer: !CI`, `retries: CI?1:0`. 신규 `tests/e2e/main.spec.ts` 2 케이스 — ① `#card-grid` visible + `.repo-card:visible` ≥ 1, ② `#search-input` 에 `zzznomatchxxx` 입력 후 400ms(filter.ts debounce 150ms 여유) 대기 후 visible 카드 수 < 초기값. `.gitignore` 에 `playwright-report/` + `test-results/` 추가. 검증: `npm run build && npm run test:e2e` 실행 시 **2 passed (10.4s, 4.1s + 4.5s)** 모두 통과. 회귀 토큰 모두 유지.

- 2026-04-26: **plan v3 Phase 8 (C-4) — sharp 이미지 정규화** (승인 10·11 묶음). `package.json` `dependencies` 에 `sharp ^0.34.5` 추가(`npm install sharp --save --include=optional` 로 lockfile 에 모든 platform 바이너리 등록 — Linux/macOS/Windows + linuxmusl + arm64 등). `.gitignore` 에 `public/thumbs/` 추가(빌드 산출물). 신규 `src/lib/thumbnail.ts` — `localizeThumbnail(repoName, imageUrl)` Map 캐시 + 외부 이미지 fetch + `sharp().resize({width:800, withoutEnlargement:true}).webp({quality:80})` + `public/thumbs/<sanitized>.webp` 저장 → `/thumbs/<repo>.webp` 경로 반환. 실패(네트워크/포맷) 시 null + try/catch 로 빌드 중단 차단. `data.ts` `buildCardData` 가 `extractFirstImage` 직후 `localizeThumbnail` 호출, 실패 시 원본 URL 폴백(ProjectCard 가 다시 Thumbnail.jpg 폴백). `.github/workflows/deploy.yml` `npm ci` → `npm ci --include=optional` (plan v3 단일화 결정 — `.npmrc optional=true` 비채택). 검증: 빌드 10.45s 정상, 회귀 토큰 모두 유지, MOCK 빌드 통과, `public/thumbs/test.webp` 가 gitignore 에 의해 정상 차단(`git check-ignore` 확인). **검증 한계**: 현재 카드 3개(MailTrayNotifier/AppGroup/SmartThingsPublic) 모두 README 첫 이미지가 없어서 dist/thumbs 시각 검증 불가(폴백 정상 = src="/Thumbnail.jpg"). sharp 변환 동작 자체는 직접 호출 테스트로 검증(GitHub avatar PNG 200x200 57295byte → WebP 7610byte = 87% 감소). 이미지가 있는 README 가 추가되면 자동으로 dist/thumbs/<repo>.webp emit.

- 2026-04-26: **plan v3 Phase 7 (C-1) — PWA (manifest + Service Worker)** (승인 7). `public/site.webmanifest` 에 plan v3 §2 C-1 명세에 따라 `id: "/"`, `start_url: "/"`, `scope: "/"` 3 필드 추가(user pages 라 모두 root 기준). 기존 `theme_color: "#00FF9D"` / `background_color: "#0B0E11"` / 192·512 maskable PNG icons 유지. 신규 `src/pages/sw.js.ts` Astro endpoint(`prerender = true`) — **CACHE_NAME = `pjc-v${Date.now().toString(36)}` 빌드 타임 base36 timestamp** 로 매 빌드마다 unique(예: `pjc-vmof5tqhv`). install: `self.skipWaiting()`. activate: 이전 CACHE_NAME 모두 `caches.delete()` + `clients.claim()` 으로 즉시 활성. fetch: same-origin 만 처리(cross-origin GitHub avatar / raw 이미지 통과), HTML(`req.mode === 'navigate'` or `Accept: text/html`) = **network-first**(실패 시 캐시 폴백), 그 외 정적 자산 = **stale-while-revalidate**(캐시 즉시 + 백그라운드 갱신). Response 의 `Cache-Control: no-cache, no-store, must-revalidate` 로 SW 본체 캐싱 차단. `src/layouts/Layout.astro` `<head>` 에 inline `<script>` 로 `'serviceWorker' in navigator` 체크 후 `window.addEventListener('load', ...)` 에서 `navigator.serviceWorker.register('/sw.js', { scope: '/' })` 등록(실패 시 console.warn). 검증: 실데이터 빌드 9.24s(이전 10.29s 대비 -1s, sw.js emit +1ms — endpoint 가 정적 문자열 반환), `dist/sw.js` 2089 byte 정상 emit + CACHE_NAME 토큰 정상, `dist/site.webmanifest` 501 byte + start_url/scope/id 모두 `/` 확인, `dist/index.html` 에 SW 등록 script 정상 포함, `dist/sitemap-*.xml` 에 sw.js 0건(Astro 가 .js endpoint 자동 제외). `MOCK_REPOS=1` 빌드도 sw.js + manifest 정상 emit. **plan v3 §2 C-1 검증 항목 중 자동 검증 가능 항목 모두 통과**: scope/start_url/sw.js 위치 일치, CACHE_NAME 빌드별 갱신, network-first/SWR 분기. **수동 검증 항목(브라우저 필요)**: Chrome DevTools → Application → Manifest/SW 정상 / 오프라인 모드 메인 페이지 로드 / SW 갱신 후 1회 새로고침 반영 / Lighthouse PWA 점수 회귀 → 다음 배포 후 사용자 확인 단계. 회귀: Phase 2/3/6 토큰 모두 유지.

- 2026-04-26: **plan v3 Phase 6 (A-4) — README 풀텍스트 검색 (lazy fetch search-index.json)** (승인 3). 신규 `src/lib/search-index.ts` — `toPlainText()` (Markdown 코드/HTML 태그/링크/헤딩/리스트 마커 제거 + 공백 normalize) + `truncateBytes()` (UTF-8 byte 기준 안전 절단, multi-byte continuation byte `0b10xxxxxx` 회피) + `SEARCH_INDEX_MAX_BYTES_PER_DOC = 4096` 상수. `data.ts` `getSearchIndex()` 신설 — `getAllRepoCards()` + `mapLimit(8)` 로 카드별 README plaintext 추출 + 4KB 절단, name/text 모두 lowercase 로 정규화. 신규 `src/pages/search-index.json.ts` Astro endpoint(`prerender = true` + `Content-Type: application/json; charset=utf-8`). `astro.config.mjs` `@astrojs/sitemap` 에 `filter: (page) => !page.endsWith('/search-index.json')` 옵션 추가. `src/scripts/filter.ts` `searchIndex: Map<string,string> | null` 변수 + `applyFilters` 의 query 분기에 `readmeHit` 추가(인덱스 미도착 시 false 폴백 = 기존 검색 동작 유지) + init 끝에 `requestIdleCallback`(폴백 setTimeout 200ms)으로 lazy fetch + 도착 시 검색어 있으면 `applyFilters()` 자동 재실행(race 조건 폴백). `withBase('/search-index.json')` 사용. 검증: 실데이터 빌드 10.29s(이전 9.86s 대비 +0.43s, search-index emit 307ms), `dist/search-index.json` **8655 byte**(250KB 임계의 **3.4%**, 카드 3개 × 평균 ~3KB), JSON 파싱 정상 3 entries, byte 절단 정확(mailtraynotifier 4096 / appgroup 4094 / smartthingspublic 357), `dist/sitemap-*.xml` 에 search-index 포함 0건(filter 정상 동작), README plaintext 에 `pop3`/`mvvm`/`winui`/`smartapps` 등 카드 메타 외 키워드 정상 포함 → 검색 매칭 가능. `MOCK_REPOS=1` 빌드 성공(카드 0건 → 인덱스 `[]` 빈 배열 emit). 번들에 'search-index' 토큰 포함, Phase 2/3 회귀 토큰 모두 유지.

- 2026-04-26: **plan v3 Phase 5 (B-3) — 상세 페이지 우측 상단에 최근 4주 commit sparkline 추가** (승인 6). `types.ts` `RepoDetailData.recentCommits?: number[]` 추가(공개 타입 옵셔널 확장). `github.ts` `fetchCommitActivity(repoName)` 신설 — Map 캐시, USE_MOCK 분기 null 폴백, **GitHub stats 캐시 202 워밍업 → 1.5s × 최대 3회 재시도 후 null**, 404/451 → null, mapLimit(8) 동시성 그대로 유지. `data.ts` `getRepoDetail` 가 `Promise.all([fetchReadme, fetchReleases, fetchCommitActivity])` 로 병렬 페칭 후 `recentCommits` 채움. 신규 `src/components/detail/ActivitySpark.astro` — props `weeks: number[]`(길이 4, 오래된→최신), viewBox 100×28 SVG polyline + 4개 circle(`<title>` `N commits` 툴팁), 좌표 `PAD=3` 으로 노드 잘림 방지. `[repo].astro` 우측 sticky-wrap 의 ReleasesCard **위에** ActivitySpark 통합, `detail.recentCommits && some((v) => v > 0)` 가드(0커밋 저장소도 미렌더 폴백). `global.css` `.activity-spark` 카드(`var(--bg-surface)`, `--bg-elevated` 보더, 0.75rem radius — ReleasesCard 와 동일 토큰) + `.activity-spark-svg` color emerald + circle fill emerald. `i18n/ko.json` `activity_spark_label`: "최근 4주 커밋", `en.json`: "LAST_4_WEEKS". 검증: 실데이터 빌드 9.86~10.21s(이전 4.56s 대비 **B-3 단독 +5.30s, 임계 +60s 의 9% 사용** — 매우 여유). GitHub stats 캐시가 미워밍업 상태라 첫 빌드는 모두 timeout → ActivitySpark 미렌더 폴백 정상 동작 확인(3개 detail HTML 모두 `.activity-spark` 0건). 정상 sparkline 마크업은 GitHub stats 백그라운드 워밍업 완료 후 다음 빌드에서 자연 노출 예정. Phase 2/3 토큰("Escape"/preventScroll/tabIndex/aria-pressed/card-chip/data-topic) 회귀 없음.

- 2026-04-26: **plan v3 Phase 4 (B-2) — InsightsPanel 에 TOP_LANGUAGES / TOP_TOPICS 두 줄 추가** (승인 4b). `types.ts` `WorkspaceInsights` 에 `topLanguages: string[]` / `topTopics: string[]` 필드 추가(공개 타입 확장). `data.ts` `collectTopLanguages`(카드별 `languages.ratio` 가중 합 기준 상위 N) + `collectTopTopics`(카드별 `topics` 등장 횟수 기준 상위 N) 신설, `collectInsights` 가 둘을 각각 3개씩 반환. `i18n/ko.json` + `i18n/en.json` 에 `insight_top_languages` / `insight_top_topics` 라벨 4건 추가(ko: "주요 언어"/"주요 토픽", en: "TOP_LANGUAGES"/"TOP_TOPICS"). `InsightsPanel.astro` 에 `hasTopLanguages`/`hasTopTopics`/`hasTops` 가드와 `.insights-tops` 마크업(두 `.insight-toplist`, 각 라벨 + `.insight-tag` 칩) 추가 — 둘 다 빈 배열이면 마크업 자체 미렌더. `global.css` `.insights-tops`(flex-basis 100% 로 메트릭 줄 다음으로 wrap, top-border 구분), `.insight-toplist`, `.insight-toplist-values`, `.insight-tag`(rgba(0,255,157,...) 음영, `data-rank='0'` 강조) 스타일 추가(디자인 토큰만 사용). 검증: 실데이터 빌드 4.24~4.56s(이전 4.68s 대비 비슷), `dist/index.html` 에 `.insights-tops` 1건 + `.insight-toplist` 2건 + `.insight-tag` 5건(C#/Groovy/Shell + windows/wpf) 정상 emit, `data-i18n="insight_top_languages|topics"` 양쪽 모두 fallback 텍스트 패턴(SCREAMING_SNAKE_CASE) 일관. `MOCK_REPOS=1` 빌드(카드 0건)에서 `.insights-panel` 은 정상 렌더(`insight-value">0` 2건) + `.insights-tops` 미렌더 폴백 정상. Phase 2/3 토큰("Escape"/preventScroll/tabIndex/aria-pressed/card-chip/data-topic) 회귀 없음.

- 2026-04-26: **plan v3 Phase 3 (A-1 + B-1) — 카드 chip 클릭 필터 토글 + 카드 푸터 언어 분포 미니 바 추가** (카드 마크업 통합 변경, 승인 1·4a·5 묶음). `types.ts` `RepoCardData.languages?: Array<{name; ratio}>` 추가(공개 타입 확장, 옵셔널이라 기존 호출자 호환). `github.ts` `fetchLanguages(repoName)` 신설 + Map 캐시 + USE_MOCK 분기에서는 null 폴백. `data.ts` `normalizeTopLanguages` 신설(상위 3개만 추출 후 셋의 합 기준 ratio 정규화 = 합 1.0), `buildCardData` 가 `Promise.all([fetchReadme, fetchLanguages])` 로 병렬 페칭. `ProjectCard.astro` chip `<span>` → `<button type="button" data-topic={lowercase} aria-pressed="false">` 로 시맨틱 변경(시각 동일), 푸터에 `.card-langs` 미니 바 마크업(role="img" + aria-label + 세그먼트별 title 툴팁) 추가. `global.css` `button.card-chip` 의 appearance reset + `aria-pressed='true'` 강조 + `.card-langs`/`.card-lang-seg[data-rank=0|1|2]` 4px 스파크라인 스타일(rgba(0,255,157,...) 음영 3단계, 디자인 토큰만 사용). `filter.ts` `grid.addEventListener('click')` 로 chip event delegation(`stopPropagation` + checkbox 토글 + applyFilters), `syncChipPressed()` 가 checkbox change/clear/URL 복원 시 chip aria-pressed 동기화. 검증: 실데이터 빌드 4.68s(이전 4.20s 대비 +0.48s, plan v3 §2 B-1 단독 +30s 임계 대비 매우 여유), 5 페이지 + 3 OG + RSS + sitemap emit 정상, dist/index.html 에 `<button class="card-chip">` 3건 + `data-topic` 모두 lowercase + `card-langs` 3건(C# 100%, Groovy 100% 등) 확인. `MOCK_REPOS=1` 빌드도 성공(languages null → 미니 바 미렌더 폴백 동작).

- 2026-04-26: **plan v3 Phase 2 (A-2) — 메인 키보드 단축키 추가**. 신규 `src/scripts/keyboard.ts` 모듈로 `/` 검색 포커스, `Esc` 검색 비우고 blur(검색창 안에서만), `j`/`k` 보이는 카드 다음/이전 이동, 카드 자체 포커스 시 `Enter` 상세 진입을 처리. 카드(`<article class="repo-card">`)는 마크업 변경 없이 첫 j/k 시점에 `tabindex="-1"` 을 런타임 부여. `INPUT/TEXTAREA/SELECT/contenteditable` 포커스 중에는 가로채지 않음(정상 입력 보장), `Ctrl/Meta/Alt` 모디파이어 조합도 절대 가로채지 않음(브라우저/OS 단축키 보호). visible 카드 판정은 filter.ts 가 숨긴 카드의 inline `style.display === 'none'` 으로 결정. `src/pages/index.astro` 의 `<script>` 에 keyboard 모듈 import 추가. README "접근성 (a11y)" 에 단축키 한 줄 + 디렉터리 구조에 `keyboard.ts` 항목 추가. 검증: `MOCK_REPOS=1 npm run build` + 실데이터 `npm run build` 모두 성공(5 페이지 + 3 OG + RSS + sitemap, 약 4.2s), dist 번들에 "Escape"/"j"/"k"/`preventScroll`/`tabIndex` 토큰 모두 포함 확인.

- 2026-04-26: **Phase 1 (A-3) 롤백 — archived / fork 숨기기 토글 기능 제거**. 사용자 요청으로 같은 날 추가된 토글을 모두 되돌리고 archived/fork 카드도 항상 표시되는 기존 동작으로 복귀. 변경: `FilterBar.astro` 토글 마크업 / `global.css` `.filter-toggle` 스타일 / `filter.ts` 토글 변수·조건·이벤트·URL 쿼리(`hideArchived`, `hideFork`) 동기화 / `ko.json`·`en.json` `filter_hide_archived`·`filter_hide_fork` 키 모두 삭제. 검증: `MOCK_REPOS=1 npm run build` 성공, `dist/index.html` 에 토글 흔적 0건, src 전수 검색에서 잔존 참조 없음. `README.md` 의 "숨기기 토글" 라인도 함께 제거.

- 2026-04-26: **Phase 1 (A-3) — archived / fork 숨기기 토글 추가**. `FilterBar.astro` 에 토글 2개 + i18n 키(`filter_hide_archived`, `filter_hide_fork`) 추가, `filter.ts` `applyFilters` 에 토글 조건 추가, `restoreFromQuery`/`syncQuery` 에 `?hideArchived=1`/`?hideFork=1` 동기화 추가, `global.css` 에 `.filter-toggle` 스타일(기존 `--emerald` / `--bg-surface` / `--bg-elevated` 토큰만 재사용). 토글 기본값 OFF, 카드 0건이어도 자동 비활성/숨김 처리 안 함(plan v3 §2 A-3 범위 제한). 검증: `MOCK_REPOS=1 npm run build` 성공, `dist/index.html` 에 두 토글 마크업 정상 렌더 확인. **2026-04-26 추가 요청으로 같은 날 롤백됨 (위 항목 참조).**

- 2026-04-25: `RepoDetailData` 확장 — `updatedAt/archived/fork` 3필드 추가 (`types.ts`). `data.ts` `getRepoDetail` 에서 채워 반환. `[repo].astro` 에서 `fetchPublicRepos` + `repoMap` 구성, `repo` prop 전달, `repo` 기준 메타 재구성(`updatedAt/archived/fork` fallback) 로직 제거. 공개 타입 *추가*만 있으므로 기존 호출자 호환. DetailHeader prop 시그니처는 그대로 유지(스코프 내 변경 최소화).

- 2026-04-25: 코드리뷰(/simplify) 반영 — 버그 2건 + 중복 제거/경량화 7건.
  - `filter.ts` 검색 대소문자 버그: `dataset.name/description` 을 원문 케이스로 비교해 대문자 포함 이름/설명이 검색에서 누락되던 문제 수정 (`toLowerCase()` 추가)
  - `ReleasesCard.astro` 잘못된 HTML 중첩(`<ul>` 직계 자식으로 `<div>`) → `.releases-timeline` div 래퍼 + 내부 `<ul class="releases-timeline-list">` 구조로 교정. `global.css` 의 `list-style/padding/margin` 규칙을 새 클래스로 이동
  - `readme.ts` `normalizeImageUrl`/`resolveImageSrc` 중복 통합 — `extractFirstImage` 에서 `rawBase` 를 구성해 `resolveImageSrc` 하나만 사용
  - `src/components/GithubIcon.astro` 신설 — Footer/ActionButtons 에 중복되던 octicon SVG path 제거
  - `src/scripts/dom.ts` 신설(`createDropdown`) — `filter.ts` Topic 패널 / `i18n.ts` 언어 메뉴의 open·close·outside-click 로직 공통화
  - `src/lib/data.ts` `getRepoMap()` 신설 — `getRepoDetail` 의 `repos.find(O(N))` 를 Map 조회(O(1))로 교체
  - `src/lib/data.ts` `getRepoMeta()` 신설 + `src/pages/og/[repo].png.ts` 교체 — OG 라우트가 불필요한 README 렌더링을 거치지 않도록 경량 meta 조회 사용
  - `i18n.ts` `applyLang` 에 동일 언어 early-return 추가 — 같은 언어 재적용 시 DOM 전체 순회·포맷 재계산 스킵
  - 검증: `MOCK_REPOS=1 npm run build` 성공

- 2026-04-24: 메인 화면 우측 "프로젝트 N개" 카운트 표시 제거 (`FilterBar.astro` 의 `#result-count` 제거). `filter.ts` 는 요소 미존재 시 자동 no-op.

- 2026-04-24: **Emerald Cybernetic 재디자인 완료**. Neo-Brutalist(라임) 테마에서 Emerald Cybernetic(에메랄드 네온 #00FF9D + Space Grotesk + glass-rim + neon glow) 로 전면 교체.
  - 토큰: `tailwind.config.mjs`, `src/styles/global.css` 전면 재작성 (`prose-dark` → `prose-emerald`)
  - Layout/Header/Footer: Top AppBar 신설(P-J-C 로고 + 검색 + 언어), Footer 1줄 축소
  - 메인: `DashboardHero` 재작성(아바타+타이틀+정렬), `InsightsPanel` **신규**(4 메트릭), `RecentReleases` 제거(파일 삭제), `FilterBar` Topic 전용, `ProjectCard` Bento 스타일
  - 상세: `DetailHeader` 재작성(상태 pulse + 버전 + Last Updated 자동 규칙), `ReadmePanel` 카드 헤더바 추가, `ReleasesCard` 타임라인, `ActionButtons` 2-col
  - 데이터: `types.ts` 에 `pushed_at`, `open_issues_count` 필드 추가. `data.ts` 에 `collectInsights()`, `getOwnerProfile()` 신규. 아바타는 `github.com/{owner}.png` 리다이렉트 활용 (추가 API 호출 없음)
  - i18n: `ko.json`/`en.json` 재작성(대시보드 톤), 시간 단위 확장(`time_hours_ago`, `time_minutes_ago`, `time_just_now`). `dictionary.ts` `formatRelative` 가 분/시간 단위까지 상대 포맷
  - 404·OG: Emerald 톤 재작성
  - `.env` 신설 (사용자 PAT 등록 → rate-limit 5000/h 로 확장)
  - 검증: `npm run build` 성공 (5 페이지 + 3 OG + RSS + sitemap, 약 19s). `git tag pre-emerald-redesign` 로 롤백 지점 확보

- 2026-04-24: **Jekyll → Astro 5 전환 완료**. bitleader-dev-HomePage 의 모든 기능을 이식하고 Neo-Brutalist 디자인은 그대로 보존.
  - 신설: `package.json`, `astro.config.mjs`, `tailwind.config.mjs`, `tsconfig.json`, `scripts/ensure-mock-stub.js`, `.env.example`, `.github/workflows/deploy.yml`, `.github/workflows/lighthouse.yml`
  - `src/styles/global.css` — 기존 `assets/css/style.css` 전면 이식 + `prose-dark`(README 렌더링) + 스킵 링크 + focus-visible 포커스 링
  - `src/lib/{types,github,readme,data,og,paths}.ts` — bitleader 데이터 레이어 이식. OWNER 를 `jongcheol-pak` 으로, HOMEPAGE_REPO 를 `jongcheol-pak.github.io` 로 교체
  - `src/i18n/{ko,en}.json + dictionary.ts` — DevHub 톤으로 사전 재작성
  - `src/scripts/{filter,i18n}.ts` — 검색/Topic 다중/정렬/북마크/페이지네이션 + 언어 전환. 헤더 언어 드롭다운(`[data-set-lang]`)에 맞춰 i18n.ts 수정
  - `src/layouts/Layout.astro` — 전역 메타/canonical/OG/RSS link/스킵 링크/토스트
  - `src/components/{Header,Footer,DashboardHero,RecentReleases,FilterBar,ProjectCard}.astro` + `detail/{DetailHeader,ReadmePanel,ReleasesCard,ActionButtons}.astro`
  - `src/pages/{index,[repo],404}.astro + rss.xml.ts + og/[repo].png.ts`
  - `public/{Thumbnail.png, favicon.svg, robots.txt}`
  - 폐기: `_layouts/`, `_projects/`, `_data/`, `_manuals/`, `assets/`, `bin/`, `Gemfile`, `Gemfile.lock`, `_config.yml`, `admin.html`, `carousel_manager.html`, `index.md`, `NOTES.md`, `scripts/local_api.py`, `scripts/create_project.py`, `_site/`, `*.log`
  - 검증: `npm run build` 성공 (5 페이지 + 3 OG PNG + sitemap + RSS, 약 5.7s)
  - 백업: git tag `pre-astro-migration` 로 전환 직전 상태 보존
